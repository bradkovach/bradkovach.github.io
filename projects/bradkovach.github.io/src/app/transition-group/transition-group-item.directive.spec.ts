import { TransitionGroupItemDirective } from './transition-group-item.directive';

describe('TransitionGroupItemDirective', () => {
	it('should be defined', () => {
		expect(TransitionGroupItemDirective).toBeDefined();
	});
});
