import { ExclusiveRangePipe } from './exclusive-range.pipe';

describe('ExclusiveRangePipe', () => {
  it('create an instance', () => {
    const pipe = new ExclusiveRangePipe();
    expect(pipe).toBeTruthy();
  });
});
