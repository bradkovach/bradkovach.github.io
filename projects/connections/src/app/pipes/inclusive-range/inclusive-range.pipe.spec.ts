import { InclusiveRangePipe } from './inclusive-range.pipe';

describe('InclusiveRangePipe', () => {
  it('create an instance', () => {
    const pipe = new InclusiveRangePipe();
    expect(pipe).toBeTruthy();
  });
});
