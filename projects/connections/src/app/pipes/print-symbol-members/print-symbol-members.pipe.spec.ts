import { PrintSymbolMembersPipe } from './print-symbol-members.pipe';

describe('PrintSymbolMembersPipe', () => {
  it('create an instance', () => {
    const pipe = new PrintSymbolMembersPipe();
    expect(pipe).toBeTruthy();
  });
});
